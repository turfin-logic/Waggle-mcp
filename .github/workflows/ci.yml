name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # â”€â”€ Lint â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install ruff
        run: pip install ruff>=0.4.0

      - name: Run ruff check
        run: ruff check src/ tests/

      - name: Run ruff format check
        run: ruff format --check src/ tests/

      - name: Check markdown links
        run: python scripts/check_markdown_links.py

      - name: Check version drift
        run: python scripts/sync_version.py --check

  # ── Tests ─────────────────────────────────────────────────────────────────
  test:
    name: Test (Python ${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
        os: [ubuntu-latest]
        include:
          # Run one Windows smoke-test to catch encoding/path issues early
          - python-version: "3.11"
            os: windows-latest

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project + dev dependencies
        run: pip install -e ".[dev]"

      - name: Verify critical dependencies
        run: |
          pip list
          python -c "import networkx; print('networkx OK')"
          python -c "import pytest; print('pytest OK')"
          python -c "import waggle; print('waggle OK')"

      # Use deterministic embeddings so tests never require a 420 MB download
      - name: Run tests (deterministic mode, no ML)
        env:
          WAGGLE_MODEL: deterministic
          PYTHONUTF8: "1"   # prevents UnicodeEncodeError on Windows
        run: pytest -v --tb=long

      - name: Build local wheel for Action smoke
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        run: python -m pip wheel --no-deps --no-build-isolation --wheel-dir dist .

      # This proves the install path works, not that this exact version is live on PyPI.
      - name: Local-wheel Action install smoke
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
        run: python dist/waggle-context-action/tests/local_wheel_install_smoke.py dist/*.whl

  # ── Code-analysis tests ───────────────────────────────────────────────────
  code-analysis:
    name: Code-analysis (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project + dev + code-analysis
        run: pip install -e ".[dev,code-analysis]"

      - name: Verify critical dependencies
        run: |
          python -c "import waggle.code_extraction; print('code_extraction OK')"
          python -c "import tree_sitter_language_pack; print('tree-sitter OK')"

      - name: Run code-extraction tests
        run: pytest -v --tb=long tests/test_code_extraction.py

  # ── Packaging smoke-test ───────────────────────────────────────────────────
  package:
    name: Package smoke-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install build
        run: pip install build

      - name: Build sdist + wheel
        run: python -m build

      - name: Check distribution metadata
        run: |
          pip install twine
          twine check dist/*.whl dist/*.tar.gz

  codex-plugin-runtime:
    name: Codex plugin runtime layout
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate bundled runtime layout
        run: python scripts/build_codex_plugin_runtime.py

  # â”€â”€ VS Code extension â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  vscode-extension:
    name: VS Code extension validation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/vscode-extension

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/vscode-extension/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Compile and smoke-test extension
        run: npm run test:smoke

      - name: Run command validation
        run: node scripts/validate-commands.js

      - name: Run regression tests
        run: npm run test:regression

  graph-studio:
    name: Graph Studio validation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mcp/graph-ui

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/mcp/graph-ui/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Graph Studio
        run: npm run build

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run browser tests
        run: npm run test:browser

      - name: Run unit tests
        run: npm run test:unit

  claude-extension:
    name: Claude Desktop extension validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: apps/mcp/claude-desktop-extension

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/mcp/claude-desktop-extension/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Validate MCPB bundle
        run: npm run validate

      - name: Check metadata version alignment
        run: python3 ../../../scripts/check_extension_metadata.py --verbose
