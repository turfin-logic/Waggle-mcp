# Waggle WebMCP submission assets

These assets are prepared for the README, Devpost gallery, and demo-video
overlays. The Workspace and Graph Studio captures were taken from the public
challenge deployment at `2026-08-27T08:25:14Z` in one isolated seeded browser
session. The current governance capture was taken from the same branch UI
against a temporary deterministic local Waggle backend and must be replaced
with the public capture described below before submission.

| Asset | Source and verified claim | Suggested alt text |
|---|---|---|
| `01-waggle-webmcp-cover.png` | Live Workspace overview. Shows Challenge Demo, Human controlled, Current Context, Key Decisions, the real Memory Map preview, Recent Memories, and Pending Human Review. | Waggle Workspace showing shared project context, authoritative decisions, recent memories, and a live memory graph. |
| `03-proposal-human-governance.png` | Temporary local capture of the real deterministic governance flow. Replace it with a live public capture before submission. | Waggle proposal review showing the exact human-approved SQLite decision frozen for later application by ChatGPT. |
| `04-graph-studio-lineage.png` | Live Graph Studio from the same session, filtered to the real seeded `Current memory workflow —updates→ Previous memory workflow` lineage. | Waggle Graph Studio focused on current and previous memory workflow nodes connected by an updates edge. |
| `05-webmcp-architecture.png` | Submission-ready 1600×900 raster architecture graphic. | Human and ChatGPT meet in the Waggle Workspace, where four WebMCP tools cross a human approval boundary before updating an isolated SQLite-backed MemoryGraph. |
| `05-webmcp-architecture.svg` | Editable vector source for the architecture graphic. | Same as the PNG architecture graphic. |

## Required public recapture

- Capture `02-workspace-guided-demo.png` from the public deployment after the
  authoritative storage recall advances the live guide.
- Replace `03-proposal-human-governance.png` from the same public session after
  the exact human-approved value is frozen.
